import { Spinner } from 'flowbite-react';
import './Loading.css';

function Loading() {
    return (
        <div className="loading">
            <Spinner />
        </div>
    );
}

export default Loading;
